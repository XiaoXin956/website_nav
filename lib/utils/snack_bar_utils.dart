

import 'package:flutter/material.dart';
import 'package:website_nav/main.dart';

showSnackBar({String msg=""}){

  // if(appGlobalKey.currentContext!=null){
  //   ScaffoldMessenger.of(appGlobalKey.currentContext!).clearSnackBars();
  //   ScaffoldMessenger.of(appGlobalKey.currentContext!).showSnackBar(SnackBar(content: Text(msg)));
  // }

}